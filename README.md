## 🚀Getting Started
1. Install project dependencies
  ```bash
  npm install
  ```
2. Run the development server in project root path:

  ```bash
  npm run dev
  ```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.
